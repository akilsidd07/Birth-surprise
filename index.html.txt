<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday | For You</title>
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Poppins:wght@300;400;600&family=Playfair+Display:ital,wght@0,700;1,700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --pink: #FFB6C1;
            --purple: #A855F7;
            --gold: #FFD700;
            --white: #FFFFFF;
            --glass: rgba(255, 255, 255, 0.2);
            --glass-border: rgba(255, 255, 255, 0.4);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            scroll-behavior: smooth;
        }

        body {
            background: linear-gradient(135deg, #fdfcfb 0%, #e2d1c3 100%);
            font-family: 'Poppins', sans-serif;
            color: #333;
            overflow-x: hidden;
        }

        /* --- Background Sparkles --- */
        #sparkle-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        /* --- Navigation / Music --- */
        nav {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
        }

        .music-ctrl {
            background: var(--glass);
            backdrop-filter: blur(10px);
            border: 1px solid var(--glass-border);
            padding: 10px 20px;
            border-radius: 50px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            transition: 0.3s;
        }

        /* --- Hero Section --- */
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            position: relative;
            z-index: 1;
        }

        .hero h1 {
            font-family: 'Playfair Display', serif;
            font-size: clamp(3rem, 10vw, 6rem);
            background: linear-gradient(to right, var(--purple), var(--pink), var(--gold));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 20px;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 1.5s forwards 0.5s;
        }

        .typewriter {
            font-size: 1.5rem;
            min-height: 1.6em;
            color: #555;
        }

        /* --- Countdown --- */
        .countdown-container {
            margin-top: 50px;
            display: flex;
            gap: 20px;
        }

        .count-box {
            background: var(--glass);
            backdrop-filter: blur(15px);
            border: 1px solid var(--glass-border);
            padding: 20px;
            border-radius: 15px;
            min-width: 80px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.1);
        }

        .count-box span {
            display: block;
            font-size: 2rem;
            font-weight: 600;
            color: var(--purple);
        }

        /* --- Photo Gallery --- */
        .section-title {
            text-align: center;
            font-family: 'Playfair Display', serif;
            font-size: 3rem;
            margin: 100px 0 50px;
        }

        .gallery {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            padding: 0 5%;
        }

        .photo-card {
            height: 350px;
            border-radius: 20px;
            overflow: hidden;
            position: relative;
            border: 5px solid white;
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
            transition: 0.5s;
        }

        .photo-card:hover {
            transform: scale(1.05) rotate(2deg);
        }

        .photo-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* --- Memory Lane Timeline --- */
        .timeline {
            position: relative;
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
        }

        .timeline::after {
            content: '';
            position: absolute;
            width: 4px;
            background: var(--pink);
            top: 0; bottom: 0; left: 50%;
            margin-left: -2px;
        }

        .memory-item {
            padding: 10px 40px;
            position: relative;
            width: 50%;
            opacity: 0;
            transition: 1s;
        }

        .memory-item.left { left: 0; text-align: right; }
        .memory-item.right { left: 50%; text-align: left; }

        .memory-content {
            background: var(--white);
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
        }

        /* --- Cake Section --- */
        .cake-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 100px 0;
        }

        .cake {
            width: 200px;
            height: 120px;
            background: #f4d03f;
            border-radius: 10px 10px 0 0;
            position: relative;
            box-shadow: inset 0 -15px rgba(0,0,0,0.1);
        }

        .cake::before {
            content: "";
            position: absolute;
            width: 220px;
            height: 20px;
            background: #e67e22;
            bottom: -20px;
            left: -10px;
            border-radius: 5px;
        }

        .candle {
            width: 10px;
            height: 40px;
            background: var(--purple);
            position: absolute;
            top: -40px;
            left: 50%;
            transform: translateX(-50%);
            cursor: pointer;
        }

        .flame {
            width: 15px;
            height: 20px;
            background: orange;
            border-radius: 50% 50% 20% 20%;
            position: absolute;
            top: -20px;
            left: -2.5px;
            animation: flicker 0.5s infinite alternate;
        }

        .flame.off { display: none; }

        /* --- Gift Boxes --- */
        .gift-grid {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin: 50px 0;
            flex-wrap: wrap;
        }

        .gift-box {
            width: 150px;
            height: 150px;
            background: var(--gold);
            position: relative;
            cursor: pointer;
            display: flex;
            justify-content: center;
            align-items: center;
            border-radius: 10px;
            font-weight: bold;
            transition: 0.3s;
        }

        .gift-box:hover { transform: translateY(-10px); }

        /* --- Modal --- */
        .modal {
            position: fixed;
            inset: 0;
            background: rgba(0,0,0,0.8);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 2000;
            backdrop-filter: blur(10px);
        }

        .modal-content {
            background: white;
            padding: 40px;
            border-radius: 20px;
            max-width: 500px;
            text-align: center;
            position: relative;
        }

        /* --- Animations --- */
        @keyframes fadeInUp {
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes flicker {
            from { transform: scale(1); opacity: 0.8; }
            to { transform: scale(1.2); opacity: 1; }
        }

        @keyframes float {
            0% { transform: translateY(0) rotate(0); }
            50% { transform: translateY(-20px) rotate(5deg); }
            100% { transform: translateY(0) rotate(0); }
        }

        .balloon {
            position: absolute;
            width: 50px;
            height: 70px;
            background: var(--pink);
            border-radius: 50%;
            z-index: -1;
            animation: float 6s infinite ease-in-out;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .timeline::after { left: 40px; }
            .memory-item { width: 100%; padding-left: 70px; text-align: left !important; }
            .memory-item.right { left: 0; }
        }
    </style>
</head>
<body>

    <canvas id="sparkle-canvas"></canvas>

    <nav>
        <div class="music-ctrl" onclick="toggleMusic()">
            <span id="music-icon">🎵</span>
            <small>Play Music</small>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <h1 id="main-title">Happy Birthday, [NAME]!</h1>
        <div class="typewriter" id="welcome-msg"></div>
        
        <div class="countdown-container">
            <div class="count-box"><span id="days">00</span><small>Days</small></div>
            <div class="count-box"><span id="hours">00</span><small>Hours</small></div>
            <div class="count-box"><span id="minutes">00</span><small>Mins</small></div>
            <div class="count-box"><span id="seconds">00</span><small>Secs</small></div>
        </div>
    </section>

    <!-- Gallery Section -->
    <h2 class="section-title">A Year of You</h2>
    <div class="gallery">
        <div class="photo-card"><img src="https://images.unsplash.com/photo-1513151233558-d860c5398176?w=500" alt="Memory 1"></div>
        <div class="photo-card"><img src="https://images.unsplash.com/photo-1464349153735-7db50ed83c84?w=500" alt="Memory 2"></div>
        <div class="photo-card"><img src="https://images.unsplash.com/photo-1530103043960-ef38714abb15?w=500" alt="Memory 3"></div>
        <div class="photo-card"><img src="https://images.unsplash.com/photo-1496333039240-391397e5ecad?w=500" alt="Memory 4"></div>
    </div>

    <!-- Timeline Section -->
    <h2 class="section-title">Memory Lane</h2>
    <div class="timeline">
        <div class="memory-item left" id="mem1">
            <div class="memory-content">
                <h3>The First Time We Met</h3>
                <p>A moment I'll never forget. The world seemed a bit brighter.</p>
            </div>
        </div>
        <div class="memory-item right" id="mem2">
            <div class="memory-content">
                <h3>Our Best Adventure</h3>
                <p>Remember that time we got lost? I wouldn't trade it for anything.</p>
            </div>
        </div>
    </div>

    <!-- Cake Section -->
    <div class="cake-container">
        <h2 class="section-title">Make a Wish</h2>
        <div class="cake">
            <div class="candle" id="candle" onclick="blowCandle()">
                <div class="flame" id="flame"></div>
            </div>
        </div>
        <p style="margin-top: 50px;">(Click the flame to blow out the candle)</p>
    </div>

    <!-- Surprise Section -->
    <h2 class="section-title">Unwrap Your Gifts</h2>
    <div class="gift-grid">
        <div class="gift-box" onclick="showSurprise('You are the kindest soul I know.')">🎁 Reveal 1</div>
        <div class="gift-box" onclick="showSurprise('I am so proud of everything you achieved this year.')">🎁 Reveal 2</div>
        <div class="gift-box" onclick="showSurprise('Next year is going to be even better!')">🎁 Reveal 3</div>
    </div>

    <div style="text-align: center; margin: 100px 0;">
        <button onclick="openFinalModal()" style="padding: 20px 40px; border-radius: 50px; border: none; background: var(--purple); color: white; font-size: 1.2rem; cursor: pointer; box-shadow: 0 10px 20px rgba(0,0,0,0.2);">Open Your Final Surprise</button>
    </div>

    <!-- Modal -->
    <div class="modal" id="surpriseModal" onclick="closeModal()">
        <div class="modal-content" onclick="event.stopPropagation()">
            <h2 style="font-family: 'Dancing Script', cursive; font-size: 3rem; color: var(--purple);">Surprise! 🎊</h2>
            <p id="modalText" style="margin: 20px 0; font-size: 1.2rem;"></p>
            <button onclick="closeModal()" style="background: var(--pink); border: none; padding: 10px 20px; border-radius: 5px; cursor: pointer;">Close</button>
        </div>
    </div>

    <!-- Music (Placeholder URL) -->
    <audio id="bgMusic" loop>
        <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
    </audio>

    <script>
        // --- Typewriter Effect ---
        const text = "Wishing you a day filled with love, laughter, and endless magic...";
        let i = 0;
        function typeWriter() {
            if (i < text.length) {
                document.getElementById("welcome-msg").innerHTML += text.charAt(i);
                i++;
                setTimeout(typeWriter, 50);
            }
        }
        typeWriter();

        // --- Countdown Timer ---
        // Set date to your person's birthday
        const birthday = new Date("Dec 31, 2024 00:00:00").getTime();

        setInterval(() => {
            const now = new Date().getTime();
            const diff = birthday - now;

            const d = Math.floor(diff / (1000 * 60 * 60 * 24));
            const h = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const m = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
            const s = Math.floor((diff % (1000 * 60)) / 1000);

            document.getElementById("days").innerHTML = d < 10 ? "0"+d : d;
            document.getElementById("hours").innerHTML = h < 10 ? "0"+h : h;
            document.getElementById("minutes").innerHTML = m < 10 ? "0"+m : m;
            document.getElementById("seconds").innerHTML = s < 10 ? "0"+s : s;
        }, 1000);

        // --- Sparkle Canvas ---
        const canvas = document.getElementById('sparkle-canvas');
        const ctx = canvas.getContext('2d');
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        let particles = [];
        class Particle {
            constructor() {
                this.x = Math.random() * canvas.width;
                this.y = Math.random() * canvas.height;
                this.size = Math.random() * 2 + 1;
                this.speedX = Math.random() * 1 - 0.5;
                this.speedY = Math.random() * 1 - 0.5;
                this.color = `rgba(255, 215, 0, ${Math.random()})`;
            }
            update() {
                this.x += this.speedX;
                this.y += this.speedY;
            }
            draw() {
                ctx.fillStyle = this.color;
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fill();
            }
        }

        function initParticles() {
            for(let i=0; i<100; i++) particles.push(new Particle());
        }

        function animate() {
            ctx.clearRect(0,0, canvas.width, canvas.height);
            particles.forEach(p => {
                p.update();
                p.draw();
            });
            requestAnimationFrame(animate);
        }
        initParticles();
        animate();

        // --- Interactivity ---
        function blowCandle() {
            document.getElementById('flame').classList.add('off');
            showSurprise("Make your wish come true! ✨");
        }

        function showSurprise(msg) {
            document.getElementById('modalText').innerText = msg;
            document.getElementById('surpriseModal').style.display = 'flex';
        }

        function openFinalModal() {
            showSurprise("You are the most special person in my life. Happy Birthday! ❤️");
        }

        function closeModal() {
            document.getElementById('surpriseModal').style.display = 'none';
        }

        function toggleMusic() {
            const music = document.getElementById('bgMusic');
            const icon = document.getElementById('music-icon');
            if (music.paused) {
                music.play();
                icon.innerText = "⏸️";
            } else {
                music.pause();
                icon.innerText = "🎵";
            }
        }

        // --- Scroll Reveal ---
        window.addEventListener('scroll', () => {
            const items = document.querySelectorAll('.memory-item');
            items.forEach(item => {
                const rect = item.getBoundingClientRect();
                if(rect.top < window.innerHeight - 100) {
                    item.style.opacity = "1";
                    item.style.transform = "translateY(0)";
                }
            });
        });

        // Generate Random Balloons
        function createBalloons() {
            for(let i=0; i<15; i++) {
                let b = document.createElement('div');
                b.className = 'balloon';
                b.style.left = Math.random() * 100 + "vw";
                b.style.top = Math.random() * 100 + "vh";
                b.style.backgroundColor = `hsl(${Math.random() * 360}, 70%, 80%)`;
                b.style.animationDelay = Math.random() * 5 + "s";
                document.body.appendChild(b);
            }
        }
        createBalloons();
    </script>
</body>
</html>